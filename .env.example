# Vite environment variables

# Backend API base URL (ignored if VITE_USE_MOCK=true)
VITE_API_BASE=http://localhost:4000/api

# Toggle mock/offline mode (true/false)
# IMPORTANT: When true, frontend uses mock data and DOES NOT hit backend; no data will be stored in MongoDB.
VITE_USE_MOCK=false

# Explicit socket host for chat WebSocket (helps when hostname/HTTPS differs)
# Usually http://localhost:4000; set to your backend URL if different
VITE_SOCKET_HOST=http://localhost:4000

# Optional: Firebase config if used elsewhere in the project
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
VITE_FIREBASE_MEASUREMENT_ID=
